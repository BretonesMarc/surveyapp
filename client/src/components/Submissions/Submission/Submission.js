import React from "react";

import useStyles from './styles';
const Submission = () => {   
     const classes = useStyles();

    return (
        <h1>SUBMISSION</h1>
    );
}

export default Submission;